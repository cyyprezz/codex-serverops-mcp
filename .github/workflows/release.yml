name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build and verify / Windows / Python 3.12
    runs-on: windows-latest
    steps:
      - name: Check out tagged source
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Require a tagged main commit
        shell: pwsh
        run: |
          git fetch origin main --no-tags
          git merge-base --is-ancestor $env:GITHUB_SHA origin/main
          if ($LASTEXITCODE -ne 0) { throw "Release tag must already be contained in main." }

      - name: Set up uv and Python
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "0.11.28"
          python-version: "3.12"
          activate-environment: true

      - name: Test, lint and build tagged source
        shell: pwsh
        run: |
          uv sync --locked --all-groups --python 3.12
          $env:PYTHONPATH = "src"
          python scripts/verify_release.py --tag $env:GITHUB_REF_NAME --automated-gates-only
          python -m unittest discover -s tests -v
          ruff check .
          uv build

      - name: Verify distributions and checkout-free commands
        shell: pwsh
        run: |
          $wheel = Get-ChildItem -LiteralPath dist -Filter *.whl | Select-Object -First 1
          $sdist = Get-ChildItem -LiteralPath dist -Filter *.tar.gz | Select-Object -First 1
          python scripts/verify_distribution.py $wheel.FullName $sdist.FullName

          $extract = Join-Path $env:RUNNER_TEMP "serverops-release-sdist"
          New-Item -ItemType Directory -Force -Path $extract | Out-Null
          tar -xf $sdist.FullName -C $extract
          $source = Get-ChildItem -LiteralPath $extract -Directory | Select-Object -First 1
          uv build --wheel --out-dir dist/from-sdist $source.FullName
          $rebuilt = Get-ChildItem -LiteralPath dist/from-sdist -Filter *.whl | Select-Object -First 1
          python scripts/verify_distribution.py $wheel.FullName $rebuilt.FullName

          $smokeCache = Join-Path $env:RUNNER_TEMP "serverops-wheel-cache"
          python scripts/stdio_surface_smoke.py $wheel.FullName --cache-dir $smokeCache
          uvx --offline --cache-dir $smokeCache --from $wheel.FullName serverops-install --version
          python scripts/stdio_surface_smoke.py $wheel.FullName --offline --cache-dir $smokeCache

      - name: Store verified release distributions
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: python-distributions
          path: |
            dist/*.whl
            dist/*.tar.gz
          if-no-files-found: error
          retention-days: 7

  publish-pypi:
    name: Publish verified distributions to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/codex-serverops-mcp
    permissions:
      id-token: write
    steps:
      - name: Download verified distributions
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: python-distributions
          path: dist

      - name: Publish with PyPI Trusted Publishing
        uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b # release/v1
        with:
          packages-dir: dist
          print-hash: true
          attestations: true

  github-release:
    name: Create GitHub release
    needs: publish-pypi
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download published distributions
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: python-distributions
          path: dist

      - name: Create release from the published tag
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" dist/*.whl dist/*.tar.gz \
            --repo "$GITHUB_REPOSITORY" \
            --verify-tag \
            --generate-notes \
            --title "Codex ServerOps MCP $GITHUB_REF_NAME"
